#include "sequence.hh"
#define MAX_LENGTH 10

void evaluate (const Sequence sequence){
	size_t length = sequence.length();
	if(length<MAX_LENGTH){
		
		for(int index=0;index<length/2;index++){
				
		}
	}
}